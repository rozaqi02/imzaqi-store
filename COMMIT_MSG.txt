fix: 79 bug fixes, UI polish, mobile & accessibility improvements

Bug fixes (37 issues):
- BUG-01 to BUG-18: Scroll memory lock & cleanup, confetti timer leaks,
  focus timer cleanup, Suspense error boundary, resize listener cleanup,
  module-level steps array, empty variant guard, CartContext timer cleanup,
  FlashSalePopup localStorage guard, AssistantBubble mounted guard,
  useGlobalShortcuts ref, useHeaderShrink cleanup, useLiveStats visibility
  fix, loyalty try/catch, Testimonials alive guard, promo timer stability
- BUG-19 to BUG-40: Emoji float timer cleanup, eslint warnings, keyboard
  shortcuts ref, database logic fixes (fetchTestimonials filter chain,
  checkStockAvailability null variant guard)
- Mobile BUG-01 to BUG-17: iOS input zoom fix (13->16px),
  vh->dvh fallback across all sheets & overlays, useIsMobile compound
  query fix, deprecated addListener -> addEventListener, assistant input
  attributes (enterKeyHint, autoComplete, autoCorrect, spellCheck),
  body scroll lock on iOS assistant, bottom nav label 11->12px,
  cart close button 36->44px, will-change reset on mobile lite

UI/UX improvements:
- CheckoutSteps: clickable steps, QRIS 30-min timer, hero reveal
  animations (kicker slide, title clip-path, subtitle fade-up)
- BottomNav: 4-column grid (FAQ removed), light mode shadow gradient
- Badge 'Paling Laris' replaces 'Rameeee', compare button more prominent
- Global focus visible ring, FAB stacking preserved on mobile
- Dark/light mode parity: Status.css icon color, Pay.css coverage,
  bottom-nav shadow override, Status pill accent

Em-dash cleanup:
- Replaced all em-dashes with hyphen across 14 user-facing files

Infrastructure:
- New hero-animations.css with reveal CSS classes
- PWA manifest icon/background cleanup
- global.css: dvh utilities, motion-lite overrides, focus styles
