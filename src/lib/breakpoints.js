/** Nav switches to hamburger + slide menu (matches CSS `.desktop-only` at 1024px). */
export const COMPACT_NAV_MEDIA = "(max-width: 1024px)";

/** Phone-sized layout (bottom nav, cart sheet, checkout drawer). */
export const PHONE_LAYOUT_MEDIA = "(max-width: 720px)";

/** iPad / tablet — glass blur allowed, still uses compact hamburger nav. */
export const TABLET_LAYOUT_MEDIA = "(min-width: 721px) and (max-width: 1024px)";